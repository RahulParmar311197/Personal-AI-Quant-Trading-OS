# Global Rules

The project is personal-use first and SaaS-capable later.

Prefer:
- correctness over feature count
- reproducibility over cleverness
- deterministic controls over opaque behavior
- explicit contracts over implicit coupling
- validated evidence over confidence labels

Never use an LLM response as an unbounded execution authority.
