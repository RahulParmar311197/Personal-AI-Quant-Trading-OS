# Application
ENVIRONMENT=development
LIVE_TRADING_ENABLED=false
API_PREFIX=/api/v1

# Database and external providers are intentionally omitted until their gates are implemented.
# Never commit real credentials.

# Optional Upstox sandbox acceptance test. Keep these values out of git.
# The integration test skips unless all three required values are provided.
# UPSTOX_SANDBOX_ACCESS_TOKEN=
# UPSTOX_SANDBOX_INSTRUMENT_TOKEN=
# UPSTOX_SANDBOX_LIMIT_PRICE=
# UPSTOX_SANDBOX_BASE_URL=https://sandbox.upstox.com
